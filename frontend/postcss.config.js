// postcss.config.js
module.exports = {
    plugins: {
      // use the new Tailwind PostCSS plugin package
      '@tailwindcss/postcss': {},
      // autoprefixer stays the same
      autoprefixer: {},
    }
  }